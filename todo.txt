# MVP
- password reset
  - email sending (waiting on DNS propagation)
- figure out deployment and url / redirecting for new site
- support table pages (especially ratings)
- lesson signups
  - add unique constraint to lessons table
- on members/{id} for the past 12 months add button to show all

# Less Important
- backfill more calendarDates for older lessons
- add a higher level db permission? For managing who gets permissions
- membership processing more automation
  - use serverfns instead of raw sql copy
  - automated email sending
  - automate csv processing
- dev tool: indicator for last prod -> db sync
- automate the stuff in maintenance.md
  - quarters table
  - automated email reminder for lesson_quarter
- easily review out_to_sea statuses (or re-think its usage)
- track memberships more precisely (by every renewal instance rather than overwriting expireQtr)
- member combobox sort by frequency / MRU of some kind
- replace old lesson sign-up flow
  - involves sending the emails, wiring up to main website
- test UX on mobile
- fix overall slowness
- IMA plus one matching